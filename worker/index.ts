export interface Env { DB: D1Database; ENVIRONMENT: string; AI?: Ai; }
const json = (body: unknown, init: ResponseInit = {}) => new Response(JSON.stringify(body), { ...init, headers: { "content-type": "application/json", ...(init.headers || {}) } });
export default { async fetch(request: Request, env: Env): Promise<Response> { const url = new URL(request.url); if (request.method === "OPTIONS") return new Response(null, { headers: { "access-control-allow-origin":"*", "access-control-allow-methods":"GET,POST,OPTIONS", "access-control-allow-headers":"content-type,authorization" } }); if (url.pathname === "/api/health") return json({ ok:true, environment:env.ENVIRONMENT }); if (url.pathname === "/api/projects" && request.method === "GET") { const { results } = await env.DB.prepare("SELECT * FROM projects ORDER BY updated_at DESC LIMIT 50").all(); return json(results); } if (url.pathname === "/api/projects" && request.method === "POST") { const body = await request.json<{ id?:string; ownerId?:string; name?:string; repository?:string }>(); const id = body.id || crypto.randomUUID(); if (!body.name) return json({ error:"name is required" }, { status:400 }); await env.DB.prepare("INSERT INTO projects (id, owner_id, name, repository) VALUES (?, ?, ?, ?)").bind(id, body.ownerId || "local", body.name, body.repository || null).run(); return json({ id, ...body }, { status:201 }); } return new Response("Not found", { status:404 }); } };
